function charConverter(first, second, third){
    firstSymbol = first.toString();
    secondSymbol = second.toString();
    thirdSymbol = third.toString();

    console.log(`${thirdSymbol} ${secondSymbol} ${firstSymbol}`)
}

charConverter('a','2','g')